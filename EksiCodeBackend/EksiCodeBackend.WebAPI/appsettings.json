{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    /* Gerekli degerler eksiCode ekibinden istenebilir veya kendinize lokal bir postgres db olusturup uzerinde calisabilirsiniz. */
    //"DevConnection": "Server={HOST_IP}; port={HOST_PORT}; user id = {DB_USER}; password = {DB_PASSWORD}; database={DB_NAME};pooling = {true/false}",
    "DevConnection": "Server=database;port=5432;Username=root;password=root;database=eksicodedev;pooling=true",
    "ProdConnection": "not yet"
  },
  "AllowedHosts": "*"
}
